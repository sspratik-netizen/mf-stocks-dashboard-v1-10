{
  "name": "mf-dashboard",
  "version": "1.9.1",
  "description": "Mutual fund, market breadth, chart pattern, stock and momentum watch dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
