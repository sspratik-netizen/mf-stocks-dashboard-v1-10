{
  "name": "mf-dashboard",
  "version": "1.9.1",
  "description": "Mutual fund, market breadth, chart pattern, stock and momentum watch dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server-wrapper.js",
    "dev": "node --watch server-wrapper.js",
    "check": "node --check server.js && node --check server-wrapper.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
